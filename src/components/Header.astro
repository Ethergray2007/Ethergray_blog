---
import { getRelativeLocaleUrl } from "astro:i18n";

import IconMenuDeep from "@/assets/icons/IconMenuDeep.svg";
import IconArchive from "@/assets/icons/IconArchive.svg";
import IconSunHigh from "@/assets/icons/IconSunHigh.svg";
import IconSearch from "@/assets/icons/IconSearch.svg";
import IconMoon from "@/assets/icons/IconMoon.svg";
import IconX from "@/assets/icons/IconX.svg";

import { stripBase, stripLocale } from "@/utils/withBase";
import { useTranslations } from "@/i18n";
import config from "@/config";

const { site, features } = config;

const locale = Astro.currentLocale ?? config.site.lang;
const t = useTranslations(locale);

const relativePath = stripBase(Astro.url.pathname);

const pathWithoutTrailingSlash =
  relativePath.endsWith("/") && relativePath !== "/"
    ? relativePath.slice(0, -1)
    : relativePath;

const currentPath = stripLocale(pathWithoutTrailingSlash, locale);

const isActive = (path: string) => {
  const currentPathArray = currentPath.split("/").filter((p) => p.trim());
  const pathArray = path.split("/").filter((p) => p.trim());

  return (
    currentPath === path ||
    currentPathArray[0] === pathArray[0]
  );
};
---

<a
  id="skip-to-content"
  href="#main-content"
  class="btn btn-sm btn-ghost bg-base-100 text-primary absolute inset-s-4 -top-full z-50 shadow-md focus:top-4"
>
  {t.a11y.skipToContent}
</a>

<header class="app-layout">
  <div
    class="navbar min-h-16 border-b border-base-300 bg-base-100 px-0 sm:min-h-20"
  >
    <!-- Logo -->
    <div class="navbar-start">
      <a
        href={getRelativeLocaleUrl(locale, "")}
        class="btn btn-ghost min-h-0 px-2 text-xl font-semibold sm:text-2xl"
      >
        {site.title}
      </a>
    </div>

    <div class="navbar-end gap-1">
      <!-- Desktop navigation -->
      <nav class="hidden sm:block" aria-label="Primary navigation">
        <ul class="menu menu-horizontal items-center gap-1 px-1">
          <!-- Posts -->
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "posts")}
              class:list={{
                "menu-active": isActive("/posts"),
              }}
              class="font-medium"
            >
              {t.nav.posts}
            </a>
          </li>

          <!-- Tags -->
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "tags")}
              class:list={{
                "menu-active": isActive("/tags"),
              }}
              class="font-medium"
            >
              {t.nav.tags}
            </a>
          </li>

          <!-- About -->
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "about")}
              class:list={{
                "menu-active": isActive("/about"),
              }}
              class="font-medium"
            >
              {t.nav.about}
            </a>
          </li>

          <!-- Archives -->
          {
            features.showArchives && (
              <li>
                <a
                  href={getRelativeLocaleUrl(locale, "archives")}
                  class:list={{
                    "menu-active": isActive("/archives"),
                  }}
                  class="btn btn-ghost btn-square"
                  title={t.nav.archives}
                  aria-label={t.nav.archives}
                >
                  <IconArchive class="size-5" />
                </a>
              </li>
            )
          }

          <!-- Search -->
          {
            features.search !== false && (
              <li>
                <a
                  href={getRelativeLocaleUrl(locale, "search")}
                  class:list={{
                    "menu-active": isActive("/search"),
                  }}
                  class="btn btn-ghost btn-square"
                  title={t.nav.search}
                  aria-label={t.nav.search}
                >
                  <IconSearch class="size-5" />
                </a>
              </li>
            )
          }
        </ul>
      </nav>

      <!-- Theme -->
      {
        features.lightAndDarkMode && (
          <button
            id="theme-btn"
            type="button"
            class="btn btn-ghost btn-square"
            title={t.a11y.toggleTheme}
            aria-label={t.a11y.toggleTheme}
            aria-live="polite"
          >
            <span class="relative grid size-5 place-items-center">
              <IconMoon class="absolute size-5 scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
              <IconSunHigh class="absolute size-5 scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
            </span>
          </button>
        )
      }

      <!-- Mobile navigation -->
      <details class="dropdown dropdown-end sm:hidden">
        <summary
          class="btn btn-ghost btn-square list-none [&::-webkit-details-marker]:hidden"
          aria-label={t.a11y.openMenu}
        >
          <IconMenuDeep class="size-6" />
        </summary>

        <ul
          class="menu dropdown-content z-50 mt-3 w-56 rounded-box border border-base-300 bg-base-100 p-2 shadow-lg"
        >
          <!-- Posts -->
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "posts")}
              class:list={{
                "menu-active": isActive("/posts"),
              }}
            >
              {t.nav.posts}
            </a>
          </li>

          <!-- Tags -->
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "tags")}
              class:list={{
                "menu-active": isActive("/tags"),
              }}
            >
              {t.nav.tags}
            </a>
          </li>

          <!-- About -->
          <li>
            <a
              href={getRelativeLocaleUrl(locale, "about")}
              class:list={{
                "menu-active": isActive("/about"),
              }}
            >
              {t.nav.about}
            </a>
          </li>

          <!-- Archives -->
          {
            features.showArchives && (
              <li>
                <a
                  href={getRelativeLocaleUrl(locale, "archives")}
                  class:list={{
                    "menu-active": isActive("/archives"),
                  }}
                >
                  <IconArchive class="size-5" />
                  {t.nav.archives}
                </a>
              </li>
            )
          }

          <!-- Search -->
          {
            features.search !== false && (
              <li>
                <a
                  href={getRelativeLocaleUrl(locale, "search")}
                  class:list={{
                    "menu-active": isActive("/search"),
                  }}
                >
                  <IconSearch class="size-5" />
                  {t.nav.search}
                </a>
              </li>
            )
          }
        </ul>
      </details>
    </div>
  </div>
</header>